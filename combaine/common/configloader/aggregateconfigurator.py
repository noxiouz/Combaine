import json
from pprint import pprint

class ParsingConfigurator(object):

    def __init__(self, aggregateconf):
        try:
            pass
        except Exception as err:
            print str(err)
